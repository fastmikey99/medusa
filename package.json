{
  "name": "medusa-deployment",
  "version": "1.0.0",
  "description": "Railway deployment wrapper",
  "scripts": {
    "start": "cd app && node server.js",
    "build": "echo 'Using npm build' && cd app && npm ci --production",
    "postinstall": "echo 'Skipping postinstall'"
  },
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=9.0.0"
  },
  "packageManager": "npm@9.0.0"
}