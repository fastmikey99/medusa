{
  "name": "medusa-backend",
  "version": "1.0.0",
  "description": "Medusa Backend",
  "scripts": {
    "start": "cd railway-app && npm start",
    "build": "cd railway-app && npm install",
    "install": "echo 'Skipping root install'"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}